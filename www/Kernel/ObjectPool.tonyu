extends null;
native Object;

\pool(key,o) {
	var list=poolList(key);
	if (list) list.push(o);
}
\withdraw(key) {
	var list=poolList(key);
	return list && list.shift();
}
\poolList(k) {
	if (lists) return lists[k];
}
\setup(klass) {
	var k=klass.meta.fullName;
	lists=lists||{};
	lists[k]=[];
}
