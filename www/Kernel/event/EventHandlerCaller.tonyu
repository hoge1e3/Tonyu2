extends null;
native Tonyu;
\callEventHandler(h, args) {
    var t;
    if (h["fiber"]) {
        t=Tonyu.thread();
        t.apply(target, h, args);
        //h["fiber"].apply(target, [t].concat(args) );
        t.steps();
    } else {
        h.apply(target,args);
    }
}
