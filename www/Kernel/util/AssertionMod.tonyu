native console;
\assertIs(obj, klass, label) {
    if (obj is klass) return obj;
    if (label) label=`(${label})`; else label="";
    throw newError("notA",obj,label,klass);
}
\newError(mesg, ...args) {
    console.error(mesg, ...args);
    return new Error($Boot.R(mesg, ...args));
}
