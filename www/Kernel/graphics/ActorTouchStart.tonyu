extends EventHandler;

"field strict";
// target: Screen
\newListener(actor:Actor, f/*:Function*/) {
    var ts=target.multiLayerTouches;
    ts.add(actor);
    return {
        dispose: \{
            ts.remove(actor);
        }
    };

}
