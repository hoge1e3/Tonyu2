extends null;
includes SpriteMod;
"field strict";
var x,y,z,direction,transform;


\draw3D(camera) {
    if (p==null || typeof p==="number") detectShape();// Just for calculate size
    camera.addSprite3D(this);
    /*if (p && typeof p.draw3D==="function") {
        var transformed=camera.transform(transform);
        p.draw3D(transformed);
    } else {
        camera.addSprite3D(this);
    }*/
}
