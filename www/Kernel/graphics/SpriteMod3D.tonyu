extends null;
includes SpriteMod;
"field strict";
var x,y,z;

\draw3D(camera) {
    detectShape();// Just for calculate size
    if (p && typeof p.draw3D==="function") {
        p.draw3D(camera);
    } else {
        camera.addSprite3D(this);
    }
}
