extends BaseActor;
"field strict";
var z;
native Tonyu;
\new(params) {
    super(params);
    initSchedulerMod();
    layer=layer||defaultLayer;
    crashScale=1;
    initSpriteMod();
    if (Tonyu.runMode) addToLayer();
    initAnimMod();
}
\defaultLayer{
    return $mainLayer3D;
}
\draw3D(camera) {
    var p=camera.to2D(position);
    camera.addSprite2D(new ActorSprite{
        x=p.x, y=p.y, scaleX=p.scale, p=this.p,
        rotation, alpha, zOrder=z
    });
}
