extends Actor;
\draw(ctx) {
    //if (hidden) return;
    useObjectPool=true;
    ctx.strokeStyle=col;
    ctx.beginPath();
    ctx.moveTo(x,y);
    ctx.lineTo(tx,ty);
    ctx.stroke();
    oneframeSprite=true;
    //hidden=true;
}
