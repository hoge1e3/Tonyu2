native Box2D;

"field strict";
var wscale;
\bvec(tx,ty) {
    var b2Vec2 = Box2D.Common.Math.b2Vec2;
    return new b2Vec2(tx/wscale,ty/wscale);  
}

\defv(t,d) {
    return ( t===t && (typeof t)==="number") ? t : d;
}
