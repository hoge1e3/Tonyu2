

y=50;
x=$screenWidth/2;

count=0;
cdec=0;
while (true) {
    if (count==0 || (cdec<10 && cdec%2==1)) text=" ";
    else text=count+" Combo";
    if (cdec>0) {
        cdec--;
        if (cdec==0) count=0;
    }
    update();
}

\inc() {
    count++;
    cdec=50;
}
