p=$pat_r2b;

while (true) {
    if (cnt>0) {
        //print("OUt",cnt);
        cnt--;
        if (cnt==0) {
            $head=new Head{x,y:y-16,t:$target,maxLen,outFrom:this};
            bo();
        }
    }
    scaleX-=(scaleX-1)*0.3;
    update();
}
\accept(t) {
    out.cnt=20;
    out.maxLen=t.maxLen+1;
    bo();
}
\bo() {
    scaleX=1.2;
}