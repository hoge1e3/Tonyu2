//file("../maps/").rel("map.json").obj{a:4};
x=200;y=100;
ps=[$pat_base,$pat_sample,$pat_neko,$pat_mglogo,$pat_tonyu];
while (true) {
    
    for (var pp in ps) {
        p=pp;
        updateEx(30);
    }
}
