text="X";
fillStyle="red";
size=10;
tx=all(Batu).length*30+30;
ty=$screenHeight-50;
while (size<50) {
    size+=5;
    update();
}
for (i=0 ; i<32; i++) {
    y+=(ty-y)/5;
    x+=(tx-x)/5;
    update();
}
