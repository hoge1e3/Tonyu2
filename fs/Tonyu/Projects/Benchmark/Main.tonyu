$Boot.setFrameRate(60);
new Rept;
$panel.die();
$consolePanel.die();
while (true) {
    if (rnd(10)==0) {
        new Tama{x:rnd(400),y:rnd(400),vx:rnd()*2-1,vy:rnd()*2-1};
    }
    if(getkey(32)==1) {
        $BM.report();
    }
    update();
}
