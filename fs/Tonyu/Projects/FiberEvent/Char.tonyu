x=30;y=300;
while(x<300) {
    x++;
    update();
}
\jump(vy) {
    if (jumping) return;
    jumping=true;
    var by=y;
    while(y<=by) {
        y+=vy;
        vy++;
        update();
    }
    jumping=false;
    //y=by;
}