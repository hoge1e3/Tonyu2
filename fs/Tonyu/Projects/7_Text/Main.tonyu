// 実行 → Mainを実行

// $ で始まる変数はグローバル変数です．他のクラスからも参照できます．
$score=0;
$player=new Player;
new Status;
for (i=0 ; i<10; i++) {
    new Chaser{x:rnd($screenWidth),y:0,p:5};
    for (j=0 ; j<20 ; j++) update();
}