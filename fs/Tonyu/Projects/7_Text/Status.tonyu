// 変数textに値を設定すると，テキストを表示するオブジェクトになります．
text="Score:";
fillStyle="white";
size=40;
align="center";
x=$screenWidth/2;
y=$screenHeight/2;
while (!$player.isDead()) {
    text="Score:"+$score;
    update();
}
text="Game Over";
