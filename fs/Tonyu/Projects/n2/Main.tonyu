//$Boot.setFrameRate(120);
x=0;
y=50;
text="Actorのtextも\n\\nで改行できます\nよ";
$panel.fillText("$panel.fillText()も\n\\nで改行できます\nはい",150,200,20,"center");
print("print()文も\n\\nで改行\nできます","それから,で区切っても","改行できません","yの初期値は下の値",y);
while(true){
    x+=1;
    update();
}