if (!elem) {
    for (i=0 ; i<10; i++) {
        new NearTest{elem:true};
    }
    x=160;
    y=160;
    while (true) {
        x=floor($mouseX/32)*32;
        y=floor($mouseY/32)*32;
        all(NearTest).withins(this,100).attr{p:8};
        all(NearTest).nearests(this).attr{p:10};
        all(NearTest).maxs \(o) {
            return o.x;
        }.attr{p:11};
        update();
    }
} else {
    x=rnd(10)*32;
    y=rnd(10)*32;
    while (true) {
        p=0;
        updateEx(5);
    }
}