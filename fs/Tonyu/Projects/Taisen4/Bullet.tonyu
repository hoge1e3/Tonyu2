spd=20;
scaleX=0.3;
p=owner.p+12;
for(i=0;i<20;i++) {
    x+=cos(rotation)*spd;
    y+=sin(rotation)*spd;
    if ( (c=crashTo(Player)) && c!=owner) {
        //print(c,c.damage);
        c.damage();
        break;
    }
    if (x<0) x+=$w;
    if (x>=$w) x-=$w;
    if (y<0) y+=$h;
    if (y>=$h) y-=$h;
    
    update();
}
die();