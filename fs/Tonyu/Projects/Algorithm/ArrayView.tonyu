\draw(c) {
    c.fillStyle="white";
    c.font="20px Monospace";
    var x=200,y=200;
    if (a) {
        c.fillText(a.join(", "), x,y);
        c.fillText("V", x+j*30,y-20);
    }
}
\wait() {
    updateEx(30);
}