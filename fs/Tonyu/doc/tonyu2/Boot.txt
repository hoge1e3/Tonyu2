[[api]]

*Bootクラス

プログラム実行時に最初に起動するクラスです．次のような動作を行います．
これらの処理は実行時に自動的に行われるので，通常は呼び出す必要はありません．

実行時に作成されたBootクラスのオブジェクトはグローバル変数[[@cfrag $Boot]]に格納されます


-画面の初期化
-マウス・タッチイベントの初期化
-[[スレッド>thread]]の初期化
-Mainクラスのオブジェクトの配置
-メインループ
--スレッドの実行
--描画

-[[setFrameRate]]
-[[useRAF]]

.