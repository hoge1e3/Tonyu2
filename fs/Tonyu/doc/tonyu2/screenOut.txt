[[BaseActor]]

*screenOutメソッド

オブジェクトが画面端の外に出ているかを判定します。

**書式
<<code
screenOut(d)
>>

-[[@cfrag d]]には、画面端から何ピクセル離れたところで判定を行なうかを指定します。省略した場合0です。

**戻り値

オブジェクトが画面端から出ている場合、画面端から最も近い距離から[[@cfrag d]]を引いた値を計算し、それが非負の値であればそのまま返し、負の場合は0を返します
