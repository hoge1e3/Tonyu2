[[チュートリアル]]

[[前へ>複数のオブジェクトを出す]]←　→[[次へ>オブジェクトを宣言する際に値を設定する]]

*オブジェクトに名前を付けて値を設定する

<<code Main
a=new Chara1;
a.x=100;
a.y=100;
b=new Chara1;
b.x=100;
b.y=200;
>>
<<code Chara1
while(true){
  x++;
  update();
}
>>
ファイル呼び出しの際に呼び出したオブジェクトに名前を付けることでそれぞれの値を設定することができる。