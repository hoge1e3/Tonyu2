[[用途別リファレンス]]

[[オブジェクトを動かす]]

*左右に移動させる
<<code
x=$screenWidth/2;
y=$screenHeight/2;
while(true){
  if(getkey("right")>0)	x+=3;
  if(getkey("left")>0)	x-=3;
  update();
}
>>
画面中央に表示されているオブジェクトを矢印キーの←と→で動かすことができる。