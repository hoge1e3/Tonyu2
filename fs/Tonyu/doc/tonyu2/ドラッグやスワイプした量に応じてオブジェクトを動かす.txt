[[用途別リファレンス]]

*ドラッグやスワイプした量に応じてオブジェクトを動かす

-[[$touches]][0].vxと [[$touches]][0].vy にドラッグまたはスワイプしたときの移動量が格納される
-これらの値をxやyに加算することで移動量に応じてオブジェクトが移動する
--ゲーム画面のどこをタッチしても移動できる（オブジェクトの近くをタッチする必要はない）

<<code
x=100;y=200;
while(true) {
    x+=$touches[0].vx;
    y+=$touches[0].vy;
    update();   
}
>>


.